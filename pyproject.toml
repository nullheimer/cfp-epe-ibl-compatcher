[project]
name = "cfp-epe-ibl-compatcher"
version = "0.1.0"
readme = "README.md"
authors = [
    { name = "nullheimer", email = "121317617+nullheimer@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
cfp-epe-ibl-compatcher = "cfp_epe_ibl_compatcher:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
